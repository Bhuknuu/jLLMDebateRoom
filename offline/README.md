# Offline CLI Runner

Standalone command-line courtroom runner for local Ollama execution without Swing GUI or internet access.

## Requirements
- Java 17+
- Ollama running locally at `http://localhost:11434`

## Execution
```bash
./run_offline.sh [model_name]
```
Default model is `qwen2.5`.
